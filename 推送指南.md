# 📤 推送代码到 GitHub - 详细步骤

## 方法一：使用自动脚本（推荐，最简单）

### 步骤 1: 在 GitHub 创建仓库

1. **打开浏览器**，访问：https://github.com
2. **登录**你的 GitHub 账号（如果没有账号，先注册）
3. **点击右上角的 '+' 号**，选择 **"New repository"**
4. **填写仓库信息**：
   - **Repository name**: `universal-archive-extractor`（或你喜欢的名字）
   - **Description**: `支持多种压缩格式的在线解压工具`
   - **Visibility**: 选择 **Public**（公开，别人可以看到）或 **Private**（私有，只有你能看到）
   - ⚠️ **重要**：**不要勾选** "Initialize this repository with a README"（我们已经有了）
5. **点击 "Create repository"**

### 步骤 2: 运行推送脚本

在终端运行：

```bash
cd "/Users/wangkexin/Desktop/Cursor code/Rar"
./push-to-github.sh
```

脚本会提示你输入仓库地址，然后自动完成推送。

---

## 方法二：手动执行命令

如果你更喜欢手动操作，按照以下步骤：

### 步骤 1: 在 GitHub 创建仓库（同上）

### 步骤 2: 获取仓库地址

创建仓库后，GitHub 会显示一个页面，上面有仓库地址，格式类似：
```
https://github.com/你的用户名/仓库名.git
```

### 步骤 3: 执行推送命令

在终端运行以下命令（将 `YOUR_USERNAME` 和 `REPO_NAME` 替换为实际值）：

```bash
cd "/Users/wangkexin/Desktop/Cursor code/Rar"

# 添加远程仓库
git remote add origin https://github.com/YOUR_USERNAME/REPO_NAME.git

# 确保分支名为 main
git branch -M main

# 推送代码
git push -u origin main
```

---

## 如果遇到身份验证问题

GitHub 现在需要使用 **Personal Access Token** 而不是密码。

### 创建 Token：

1. 访问：https://github.com/settings/tokens
2. 点击 **"Generate new token"** → **"Generate new token (classic)"**
3. **设置权限**：
   - Note: `推送代码到仓库`
   - Expiration: 选择过期时间（或 No expiration）
   - **勾选 `repo`**（完整仓库访问权限）
4. 点击 **"Generate token"**
5. **复制 Token**（只显示一次，请保存好）

### 使用 Token：

当 Git 提示输入密码时：
- **Username**: 输入你的 GitHub 用户名
- **Password**: 输入刚才复制的 Token（不是你的 GitHub 密码）

---

## 验证推送是否成功

推送成功后：

1. 访问你的 GitHub 仓库页面（地址类似：`https://github.com/你的用户名/仓库名`）
2. 你应该能看到所有文件已经上传
3. 可以点击文件查看内容

---

## 常见问题

### Q: 提示 "remote origin already exists"
**A**: 说明已经设置过远程仓库，运行：
```bash
git remote set-url origin https://github.com/YOUR_USERNAME/REPO_NAME.git
```

### Q: 提示 "Authentication failed"
**A**: 需要使用 Personal Access Token，参考上面的"身份验证问题"部分

### Q: 提示 "Permission denied"
**A**: 检查仓库地址是否正确，或者仓库是私有的但你没有权限

### Q: 想使用 SSH 方式
**A**: 需要先配置 SSH key，然后使用：
```bash
git remote add origin git@github.com:YOUR_USERNAME/REPO_NAME.git
```

---

## 推送成功后

✅ 代码已经在 GitHub 上了！

下一步：
1. 查看 [DEPLOY.md](./DEPLOY.md) 了解如何部署到云端
2. 推荐使用 **Vercel** 部署（最简单，免费）
3. 部署后你的网站就可以长期使用了

