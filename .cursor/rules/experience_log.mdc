# 经验教训日志

## 2025-01-XX: OpenAI 免费计划无法设置硬性限额

### 问题
在指导用户使用 OpenAI API 进行图像分析练习时，建议用户设置硬性限额（Hard limit）为 $5 来控制成本。但用户发现免费计划（Free tier）无法设置硬性限额，系统提示 "You must be on a paid plan to manage usage limits"。

### 原因
OpenAI 的 Usage Limits 功能只对付费计划开放。免费计划用户无法在后台设置硬性限额。

### 解决方案
1. **代码层面的使用量控制**：
   - 创建 `services/usageTracker.js` 使用量追踪器
   - 在每次 API 调用前检查是否超过限额
   - 记录每次 API 调用的成本（估算或实际值）
   - 当接近限额时发出警告，达到限额时阻止调用

2. **实现要点**：
   - 设置 `MAX_COST` 为 $4.5，留出 $0.5 缓冲空间
   - 使用本地 JSON 文件（`.usage.json`）记录使用量
   - 将 `.usage.json` 添加到 `.gitignore`
   - 定期检查 OpenAI 后台的实际余额

3. **替代方案**：
   - 如果用户愿意，可以充值 $5 升级到付费计划，然后设置硬性限额
   - 但这意味着不是完全免费使用

### 经验教训
- ✅ 在提供指导前，应该先验证功能的实际可用性
- ✅ 对于免费服务，需要提供代码层面的替代方案
- ✅ 成本控制是 AI API 使用中的重要考虑因素
- ✅ 应该提供完整的实现代码，而不仅仅是说明

### 相关文件
- `services/usageTracker.js` - 使用量追踪器实现
- `services/imageAnalyzer.example.js` - 图像分析服务示例（展示如何使用追踪器）
- `Project4-图像分析练习指南.md` - 已更新，包含代码层面的成本控制方案
