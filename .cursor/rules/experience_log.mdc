# 经验日志

## 项目：通用压缩包解压工具

### 2024 - 项目创建

#### 技术选型
- **ZIP 格式**：使用 `yauzl` 纯 Node.js 库，无需系统依赖
- **RAR 格式**：使用系统 `unrar` 命令（macOS: `brew install unrar`）
- **7Z 格式**：使用系统 `7z` 或 `7za` 命令（macOS: `brew install p7zip`）
- **TAR/GZ 格式**：使用 `tar-stream` + Node.js 原生 `zlib`
- **TAR/BZ2 格式**：使用系统 `tar` 命令（Node.js 原生不支持 bz2）

#### 大文件处理
- 使用 `multer` 的 `diskStorage` 模式，支持流式上传
- 设置 `fileSize: Infinity` 实现无大小限制
- 使用 XMLHttpRequest 的 `upload.progress` 事件实现上传进度显示

#### 文件管理
- 上传文件存储在 `uploads/` 目录
- 解压文件存储在 `extracted/` 目录
- 每个解压任务使用唯一目录名（时间戳+随机数）
- 提供清理接口删除临时文件

#### 安装和部署
- **npm 依赖安装**：`npm install` 即可，所有 Node.js 库都会自动安装
- **系统工具检查**：RAR 和 7Z 需要系统工具，但 ZIP 和 TAR 格式可以立即使用
- **启动方式**：
  - 直接启动：`npm start` 或 `node server.js`
  - 使用脚本：`./start.sh`（会自动检查依赖和系统工具）
- **服务器端口**：默认 3000，可通过环境变量 PORT 修改
- **无需额外配置**：服务器启动后即可访问 http://localhost:3000

#### 测试经验
- 服务器启动后立即可用，无需额外配置
- ZIP 和 TAR 格式无需系统工具，可以直接测试
- RAR 和 7Z 格式如果系统未安装工具，会返回友好的错误提示
- 前端会自动显示上传进度和解压状态

#### 注意事项
- RAR 和 7Z 需要系统安装相应工具，但错误提示已优化，会告知用户如何安装
- 大文件解压可能需要较长时间，前端会显示加载状态
- 生产环境需要添加安全验证和文件类型检查
- 如果用户没有 Homebrew，可以手动安装 unrar 和 p7zip，或只使用 ZIP/TAR 格式

#### 2024 - unrar 安装问题解决

**问题**：用户系统没有 Homebrew，无法使用 `brew install unrar` 安装 unrar。

**解决方案**：
1. **直接下载二进制文件**：从 RARLab 官网下载 macOS 版本的 unrar 二进制文件
   - 下载地址：https://www.rarlab.com/download.htm
   - 下载命令：`curl -L -o rarosx.tar.gz "https://www.rarlab.com/rar/rarmacos-x64-624.tar.gz"`

2. **解压并安装到项目目录**：
   ```bash
   tar -xzf rarosx.tar.gz
   mkdir -p bin
   cp rar/unrar bin/unrar
   chmod +x bin/unrar
   ```

3. **修改代码支持项目本地 unrar**：
   - 修改 `extractors/rarExtractor.js`，优先检查系统路径，如果没有则使用项目 `bin/unrar`
   - 这样无需 sudo 权限，也不需要安装到系统路径

**优势**：
- 无需管理员权限
- 无需安装 Homebrew
- 项目自包含，便于部署
- 不影响系统环境

**经验**：
- 对于需要系统工具的功能，可以提供项目本地版本作为备选方案
- 检查顺序：系统路径 → 项目本地路径 → 错误提示

#### 2024 - 打包下载功能改进

**需求**：用户希望可以一键打包下载整个解压后的文件夹，而不是每个文件单独下载。

**实现方案**：
1. **后端添加打包接口**：
   - 使用 `archiver` 库将整个解压目录打包成 ZIP 文件
   - 接口路径：`GET /api/download-all/:dir`
   - 使用流式压缩，支持大文件
   - 压缩级别设置为 9（最高压缩）

2. **前端改进**：
   - 移除每个文件的单独下载按钮
   - 在文件列表顶部添加"打包下载全部"按钮
   - 点击后直接下载整个目录的 ZIP 包

**技术细节**：
- 使用 `archiver.directory()` 方法递归添加整个目录
- 设置 `res.attachment()` 和 `res.contentType()` 确保浏览器正确下载
- 保留单个文件下载接口用于兼容性，但前端不再使用

**优势**：
- 用户体验更好，一键下载所有文件
- 保持文件夹结构
- 减少多次下载的麻烦

#### 2024 - ZIP 下载文件格式问题修复

**问题**：用户下载的 ZIP 文件只有 189 字节，macOS 归档实用工具报错"格式不支持"。

**原因分析**：
1. 前端使用 `<a>` 标签直接下载，浏览器可能在响应未完成时就保存了文件
2. 大文件流式传输时，如果使用直接链接下载，可能只保存了部分响应头

**解决方案**：
1. **后端改进**：
   - 确保响应头正确设置：`Content-Type: application/zip`
   - 添加 `Cache-Control: no-cache` 防止缓存问题
   - 改进错误处理和流管理

2. **前端改进**：
   - 改用 `fetch()` + `blob()` 方式下载
   - 等待完整响应后再创建下载链接
   - 使用 `URL.createObjectURL()` 和 `URL.revokeObjectURL()` 管理内存

**关键代码**：
```javascript
// 前端下载方式
const response = await fetch(url);
const blob = await response.blob();
const downloadUrl = window.URL.createObjectURL(blob);
// 创建下载链接并触发下载
```

**验证结果**：
- ZIP 文件格式正确（使用 `file` 命令和 `unzip -t` 验证）
- 文件可以正常解压（使用 `ditto` 和 macOS 归档实用工具验证）
- 文件大小正确（4.2MB，包含所有文件）

**经验教训**：
- 对于大文件下载，必须使用 `fetch + blob` 方式，不能直接用 `<a>` 标签
- 流式传输的文件必须等待完整响应后再保存
- 使用 `URL.createObjectURL()` 后记得调用 `revokeObjectURL()` 释放内存

#### 2024 - 下载和清理接口路径问题修复

**问题**：
1. 下载失败：前端显示"下载失败:下载失败"
2. 清理失败：显示错误"The string did not match the expected pattern"

**原因分析**：
1. Express 路由 `:dir` 无法正确匹配包含特殊字符的路径
2. 前端没有对路径进行 URL 编码
3. 清理接口的路径匹配逻辑有问题，无法找到对应的上传文件

**解决方案**：
1. **路由改进**：
   - 使用 `:dir(*)` 通配符路由匹配所有字符
   - 添加路径安全检查，防止路径遍历攻击
   - 使用 `path.resolve()` 确保路径在允许的目录内

2. **前端改进**：
   - 使用 `encodeURIComponent()` 编码路径参数
   - 改进错误处理，显示更详细的错误信息
   - 添加响应类型检查
   - 添加 blob 大小检查

3. **清理接口改进**：
   - 改进路径匹配逻辑
   - 尝试多种扩展名查找上传文件
   - 添加路径安全检查

**关键代码**：
```javascript
// 后端路由
app.get('/api/download-all/:dir(*)', ...)
app.delete('/api/clean/:dir(*)', ...)

// 前端编码
const url = `/api/download-all/${encodeURIComponent(currentOutputDir)}`;
```

**验证结果**：
- ✅ 下载接口正常工作（4.2MB ZIP文件）
- ✅ ZIP文件格式正确，可以正常解压
- ✅ 清理接口正常工作
- ✅ 路径编码和安全检查正常

**经验教训**：
- Express 路由参数需要特殊处理才能匹配包含特殊字符的路径
- 前端必须对 URL 参数进行编码
- 路径操作必须添加安全检查，防止路径遍历攻击
